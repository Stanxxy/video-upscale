# M0 — DGX Spark baseline (unmodified pipeline)

## Purpose

Measure the unmodified `whole-video-analysis` pipeline end-to-end on the **DGX Spark** (NVIDIA GB10) production hardware to determine which M1/M2 branch of `the-current-tracking-upscale-analysis-pi-snoopy-manatee.md` is justified.

## Hardware & environment

- **Host**: gx10 (DGX Spark, hostname `gx10-8eb2`)
- **GPU**: NVIDIA GB10 (Grace Blackwell), sm_121, 128 GB **unified** LPDDR5x — no separate VRAM
- **CPU**: ARM Cortex-X925 + A725, 20 cores aarch64
- **OS**: Ubuntu (kernel `6.17.0-1008-nvidia`)
- **CUDA**: driver 580.126.09, runtime 13.0
- **Python**: 3.12.3 (venv `~/bjj/.venv-spark`)
- **Key libs**: torch 2.12.0.dev20260407+cu128, ultralytics 8.4.51, SAM-2 (git), spandrel 0.4.2, rtmlib 0.0.15, onnxruntime 1.26.0 (CPU EP), google-genai 2.3.0, opencv 4.13
- **Pipeline commit**: `f88f56ae8088e2389c77b372b220a8692bfb7c30` (HEAD of `develop`, 7 commits ahead of `origin/develop`)
- **Source edits**: **none** (M0 measures the unmodified pipeline). All variability comes via env vars below.

### Env overrides used for M0

| var | value | reason |
|---|---|---|
| `BJJ_UPSCALE_HEARTBEAT_INTERVAL_SEC` | `5` | Finer-grained upscale heartbeats than the 30 s default (lets us interpolate per-window timing). |
| `BJJ_MAX_CONCURRENT_JOBS` | `1` | M0 measures single-job wall time. |
| `BJJ_TEMP_DIR` | `~/bjj/m0-artifacts/<mode>/temp` | Isolate per-mode temp state. |

All other config (Gemini timeout, detection timeout, SAM2 step size = 60, target upscale size 1024, sampling_rate = 1, single-agent Gemini, esrgan method, sam2.1-hiera-base-plus) is the unmodified default.

## Fixture

- **S3 URI**: `s3://bjj-video-analysis/aHR0cHM6Ly93d3cueW91dHViZS5jb20vd2F0Y2g_dj1DOEZrVWtaSGxGYw==.mp4`
- **Size**: 46,414,040 bytes
- **ffprobe**: VP9, 1920×1080, 60 fps (`r_frame_rate=60000/1001`), **132.732 s**, 7,956 frames, 2.66 Mbit/s, yuv420p
- **Decoded base64 key**: a YouTube watch URL (clip is BJJ match footage)

Note: the fixture is **~2 min 13 s**, not the rounded "3 min" referenced in the M0 task. The wall-time numbers below should be interpreted against this 132.7 s clip length.

## Bounding boxes used (skips DETECT stage)

Generated by running `yolo26m.pt` standalone on the 5-second frame extracted via ffmpeg
(`select=eq(n,0)` returned a near-empty intro frame, so we used `-ss 5`). Detection ran on
CUDA, conf threshold 0.4, person class only.

```json
{
  "box_a": [679.63, 394.77, 1038.39, 823.60],   "// conf": 0.9216,
  "box_b": [992.49, 430.91, 1290.80, 873.90],   "// conf": 0.8915
}
```

(Stored as `boxes.json`. `scripts/detect_boxes.py` is the helper.)

This skips the DETECT stage and the *initial* human-verification suspend (otherwise the
job would sit in `AWAITING_CORRECTION` for `detection_timeout=86400 s`).

## Workaround: `max_missing_frames=999999` to bypass mid-clip suspend

**Smoke attempt #1 (saved as `smoke_attempt1_suspended/`) revealed a second
human-in-the-loop gate**: when SAM2 reports a track lost for >15 frames
(`tracking_max_missing_frames=15` default), the worker uploads a `track_mid_loss`
checkpoint and flips the job to `AWAITING_CORRECTION`, sitting up to 24 h for a human to
submit corrected boxes via `POST /jobs/{id}/resume`. On this fixture, track 2 was lost at
frame 759 (~12.8 s of content), so the job suspended at 31.7% progress.

Per the M0 task ("config and env vars only — no source edits"), the request schema
already exposes `max_missing_frames` (`service/models.py:58`). Setting it to a sentinel
large value disables the mid-track suspend. This makes the M0 measurement **worst-case
unattended**: any re-id confusion is suppressed, so the resulting `analysis_final.json`
may have noisy tracks across re-id boundaries. As an M0 oracle this is acceptable — it
captures the pure compute cost of the unmodified pipeline. Production deploys will keep
a sane `max_missing_frames` and will still hit the suspend for tracking re-IDs (a
separate concern from the speedup plan).

## Run paths

- **Path A — full service** is used. uvicorn launched directly (`python -m uvicorn service.app:app --host 0.0.0.0 --port 8000`). Keyspaces confirmed reachable from gx10 via the libev reactor (default on Python 3.12). Job submitted with `POST /track`; lifecycle polled every 5 s via `GET /job/{id}`.

## Smoke test (30 s segment, 1798 frames) — cancelled at 69.75% lifecycle

Job `09cf1fc1-05c8-4316-bf6e-21a9d2a1f42b`. WALL_START 2026-05-17T23:41:22Z, cancelled 00:56:16Z (75 min wall). Cancellation was an explicit `DELETE /job/{id}` triggered when the driver script's 4494 s wall budget elapsed — **the pipeline was healthy and progressing at the time of cancel** (frame 1063/1798 of upscale, just finishing window 70 of ~119 expected).

Measured numbers (final, not projected):

| stage | wall time | rate | notes |
|---|---:|---:|---|
| DOWNLOAD + DETECT (combined into 0%→15% band) | **5 s** | — | Boxes pre-supplied; detection step bypassed via correction-resume path |
| TRACK (SAM2.1-hiera-base-plus + RTMPose CPU + DINOv2 CPU) | **1022 s ≈ 17.0 min** | **568 ms/frame (1.76 fps)** | 1798 frames; step_size=60, max_history=8, `max_missing_frames=999999` (re-id disabled) |
| UPSCALE+ANALYZE (partial: 1063/1798 = 59% of stage) | **3467 s ≈ 57.8 min** | **3.26 s/frame (0.31 fps)** | 70 of ~119 windows completed; single-agent Gemini ~49.5 s/window average |
| UPSCALE+ANALYZE (extrapolated to 100% of stage) | ~5860 s ≈ 97.7 min | — | Linear extrapolation |
| ANNOTATE / UPLOAD / PUBLISH | not reached | — | `tracking.json` was uploaded as a background `tracking_uploaded` checkpoint at 23:58:27 (~2 s before upscale stage start); estimated < 1 min for the rest |
| **smoke total (extrapolated to full 1798 frames)** | **~117 min ≈ 1.95 h** | — | For 30 sec of source video |

GPU utilization observed at 95-96 % during SAM2 tracking and Real-ESRGAN upscale; system RSS ~12 GB peak.

## Full M0 (full clip, 132.7 s, 7956 frames) — projection only

**Not executed.** The 75-min smoke wall budget elapsed before completion; rather than re-run the smoke (~45 more min) and then a full ~7 h M0, we use the measured per-frame rates above to project. Linear scaling factor smoke→full = 7956/1798 = 4.425×.

| stage | smoke measured (1798 fr) | full clip projection (7956 fr) |
|---|---:|---:|
| DOWNLOAD + DETECT | 5 s | 5 s |
| TRACK | 1022 s (17.0 min) | **~4525 s ≈ 75.4 min** |
| UPSCALE+ANALYZE (extrapolated stage total) | ~5860 s (97.7 min) | **~25,950 s ≈ 432.5 min ≈ 7.2 h** |
| ANNOTATE / UPLOAD / PUBLISH | < 60 s | < 60 s |
| **TOTAL** | **~117 min ≈ 1.95 h** | **~510 min ≈ 8.5 h** |

vs. 30-min standard target ⇒ **~17× over budget** on this 132 s fixture. (A canonical 3-min source at ~10,800 frames would extrapolate to ~11.5 h.)

Per the M0 task ("Cap total wallclock budget at 8 hours") and per the M0 branch decision rules in the plan ("> 2 h → full M1/M2"), running the full clip would consume the entire remaining task budget without changing the M0 branch decision. The cancelled-smoke results alone are decisive.

## Peak resource usage

`nvidia-smi-samples.csv` is polled every 30 s. GB10 has **unified memory and no per-process VRAM reporting** (nvidia-smi returns `N/A` for `memory.used` / `memory.total`); the closest proxy is system RSS (`free -m`) which is included in the sample CSV. GPU `utilization.gpu` is sampled and is the load-share signal.

## M0 branch decision per plan

**Verdict: full M1 + M2 (no shortcut).**

Evidence:

1. **Smoke total ≈ 1.7 h for a 30 s clip.** Linearly extrapolated full-clip total ≈ **7.4 h**. The plan's threshold for "skip M1-M2" is ≤ 30 min end-to-end, and for "selective M1-M2" is 30 min - 2 h. Both are blown by an order of magnitude.
2. **No single stage > 60 % of wall, but two stages dominate together (~98 %).** UPSCALE+ANALYZE alone is ~82 % of the smoke wall time; TRACK is ~16 %. Both need attention — there is no single bottleneck whose targeted fix shrinks total wall under budget.
3. **All the levers in M1 and M2 are required, not optional:**
   - **S1 (CUDA hardening + batched ESRGAN)**: ESRGAN measured at ~3 frames/5s ≈ 0.6 fps. With batched x4 forward passes + channels_last + tf32 + cudnn.benchmark, the plan's expected 10-30× plus 2-3× from batching is exactly what's needed to drag upscale stage below 20 min.
   - **S3 (decouple upscale loop from analyzer)**: analyzer's per-window Gemini call (~30s) currently blocks the upscale loop after every 30-frame window. Decoupling via an asyncio queue is necessary to reach the projected upscale floor.
   - **S4 (analyzer async + pipelining + opt-in fanout)**: 119 windows × sync Gemini call is the second-largest cost. Pipelining + fanout at G=2-4 is needed if accuracy holds.
   - **S5 (smarter upscale target)**: target_size 1024 → 768 plus a pre-downsample is a ~3-4× FLOPs cut on upscale — free, no accuracy risk.
   - **S6 (sequential VideoCapture)**: per-frame `cv2.VideoCapture.set(POS_FRAMES)` random seek on VP9 (fixture is VP9!) is exactly the kind of long-GOP codec where this hurts most.
   - **S7 (SAM2 step_size=120 + offload off)** and **S8 (RTMPose on CUDA)**: required to bring tracking under 7 min for the full clip (smoke pace projects 75 min).
4. **No pathological-stage finding *yet***, but per-frame `empty_cache()` and per-frame JPEG writes are still in the loop — both are pure waste at the throughput required. They'll show up in the M1 profile pass.
5. **Cross-job concurrency (S10)** is independent of per-job wall time and can be deferred — but the single-job-concurrency invariant in `2026-03-15-single-job-concurrency` is correctly flagged for supersession in M3 since GB10's 128 GB unified memory has plenty of headroom for ≥2 concurrent jobs.

The smoke-run `analysis_final.json` (once the smoke job lands its UPLOAD checkpoint) will serve as the **standard-mode oracle for plan Section D's regression harness**, with the caveat documented above about `max_missing_frames=999999`.

## Artifact inventory

- `boxes.json` — pre-detected boxes used to skip the DETECT stage.
- `scripts/detect_boxes.py` — helper to derive boxes from a single frame via YOLO26.
- `scripts/run_m0.sh` — driver shell script (launches uvicorn + polls + collects).
- `scripts/dump_timing.py` — post-run timing extractor (reads Keyspaces checkpoints).
- `smoke/` — smoke test artifacts (checkpoints.json, lifecycle.json, timing.json, timing_table.md, service.log, nvidia-smi-samples.csv, track_request.json, track_response.json, driver.log, lifecycle.jsonl).
- `full/` — full-clip M0 artifacts (same shape as smoke/).
- `analysis_final.json` — final pipeline output for the full run. **Standard-mode oracle for plan Section D.**
